# Backend configuration
# Preferred port for the Express API
BACKEND_PORT=3001

# CORS configuration
# Allowed frontend origin (defaults to http://localhost:3000 if not set)
CORS_ORIGIN=http://localhost:3000

# Database configuration (preferred)
# If not set, the app will attempt to read from ../database/db_connection.txt (psql ... postgresql://...)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME
