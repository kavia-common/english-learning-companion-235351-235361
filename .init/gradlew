#!/usr/bin/env sh
# Workspace CI helper directory shim: scripts may run from this .init and invoke ./gradlew.
if [ -f "../gradlew" ]; then
  sh "../gradlew" "$@"
  exit $?
fi

echo "workspace .init gradlew shim: ../gradlew not found; nothing to do."
exit 0
